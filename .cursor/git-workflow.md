# Git工作流程配置

## 🎯 项目Git工作流程规范

### 分支策略
```
main (受保护分支)
├── feature/*     # 新功能开发
├── bugfix/*      # bug修复
├── hotfix/*      # 紧急修复
└── release/*     # 发布准备
```

### 工作流程
1. **创建功能分支**: 从main创建开发分支
2. **开发代码**: 在功能分支上进行开发
3. **提交更改**: 遵循约定式提交规范
4. **推送分支**: 推送到远程仓库
5. **创建PR**: 在GitHub上创建Pull Request
6. **代码审查**: 团队成员进行代码审查
7. **合并代码**: 通过审查后合并到main
8. **清理分支**: 删除已合并的功能分支

## 📋 当前项目状态

### 活跃分支
- **feature/linux-commands-update**: Linux命令学习资料更新
  - 状态: 🚧 开发中
  - 提交: 3个本地提交待推送
  - 包含: 新增TOP25命令文档，删除工作流指南

### 分支保护规则 (建议)
```json
{
  "branch": "main",
  "protection": {
    "require_pr_reviews": true,
    "require_status_checks": false,
    "restrict_pushes": true,
    "require_up_to_date_branches": true
  }
}
```

## 🚀 常用Git命令

### 分支管理
```bash
# 创建并切换到新分支
git checkout -b feature/new-feature

# 查看所有分支
git branch -a

# 删除本地分支
git branch -d feature/completed-feature

# 删除远程分支
git push origin --delete feature/completed-feature
```

### 提交规范
```bash
# 功能提交
git commit -m "feat: 添加用户登录功能"

# 修复提交
git commit -m "fix: 修复登录页面样式问题"

# 文档提交
git commit -m "docs: 更新API文档"

# 样式提交
git commit -m "style: 格式化代码缩进"
```

### 同步操作
```bash
# 拉取最新代码
git pull origin main

# 推送功能分支
git push -u origin feature/my-feature

# 变基到最新main
git checkout feature/my-branch
git rebase main
```

## 📝 提交信息格式

### 格式规范
```
<type>(<scope>): <subject>

<body>

<footer>
```

### 类型定义
- `feat`: 新功能
- `fix`: 修复bug
- `docs`: 文档更新
- `style`: 代码格式调整
- `refactor`: 重构
- `test`: 测试相关
- `chore`: 构建或工具配置

### 示例
```
feat(auth): 添加JWT令牌验证

- 实现用户登录接口
- 添加令牌生成和验证逻辑
- 更新API文档

Closes #123
```

## 🔄 冲突解决流程

1. **拉取最新代码**
   ```bash
   git checkout main
   git pull origin main
   ```

2. **切换到功能分支**
   ```bash
   git checkout feature/my-branch
   ```

3. **变基解决冲突**
   ```bash
   git rebase main
   # 手动解决冲突文件
   git add <resolved-files>
   git rebase --continue
   ```

4. **强制推送**
   ```bash
   git push --force-with-lease
   ```

## 🎯 项目特定配置

### 忽略文件 (.gitignore)
```
# IDE配置
.vscode/
.idea/
*.swp
*.swo

# Python
__pycache__/
*.pyc
*.pyo
.env
venv/

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*

# 日志文件
*.log
logs/

# 临时文件
tmp/
temp/
```

### Git配置建议
```bash
# 设置用户信息
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"

# 设置默认推送行为
git config --global push.default simple

# 设置默认编辑器
git config --global core.editor "code --wait"
```

## 📊 当前分支状态

```
分支: feature/linux-commands-update
状态: 领先远程 3 个提交
├── afecd87 docs: 删除Git工作流程指南文档
├── 154a3ba docs: 添加Linux最常用命令TOP25精选合集
└── 7e9cefc docs: 更新README，添加Git工作流程指南链接
```

## 🚨 重要提醒

1. **不要直接推送到main分支**
2. **定期拉取最新代码避免冲突**
3. **提交前先运行测试**
4. **PR描述要清晰说明更改内容**
5. **及时响应代码审查意见**

## 📈 工作流程检查清单

### 创建新功能前
- [ ] 从main拉取最新代码
- [ ] 创建描述性的功能分支
- [ ] 在分支上进行开发

### 提交代码前
- [ ] 代码符合项目规范
- [ ] 添加必要的测试
- [ ] 更新相关文档
- [ ] 运行代码检查

### 创建PR前
- [ ] 推送功能分支到远程
- [ ] 填写详细的PR描述
- [ ] 添加相关标签和审查者
- [ ] 链接相关issue

### 合并PR前
- [ ] 通过所有代码审查
- [ ] 解决所有讨论点
- [ ] CI/CD检查通过
- [ ] 获得审批权限

---

*此配置文件为项目特定Git工作流程规范，如有变更请及时更新。*
