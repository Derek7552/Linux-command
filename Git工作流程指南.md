# Git分支开发和PR工作流程指南

## 🎯 目标
建立规范的Git开发流程，避免直接推送到main分支，通过Pull Request进行代码审查。

## 📋 分支策略

### 主分支 (main/master)
- **保护分支**: 不允许直接推送
- **用途**: 存放稳定、可发布的代码
- **合并方式**: 只能通过PR合并

### 开发分支 (feature/*)
- **命名规范**: `feature/功能描述` 或 `feat/功能描述`
- **用途**: 开发新功能或修复bug
- **生命周期**: 开发完成后删除

### 其他分支类型
- `hotfix/*`: 紧急修复分支
- `release/*`: 发布分支
- `bugfix/*`: bug修复分支

## 🚀 工作流程

### 1. 创建功能分支
```bash
# 从main分支创建新分支
git checkout main
git pull origin main
git checkout -b feature/new-feature

# 或者直接创建并切换
git checkout -b feature/new-feature
```

### 2. 在分支上开发
```bash
# 进行代码修改
echo "新功能代码" > new-feature.txt

# 添加文件到暂存区
git add .

# 提交更改
git commit -m "feat: 添加新功能

- 添加了XXX功能
- 修复了XXX问题
- 更新了XXX文档"
```

### 3. 推送分支到远程
```bash
# 推送新分支到远程
git push -u origin feature/new-feature

# 后续推送使用
git push
```

### 4. 创建Pull Request
1. 打开GitHub仓库页面
2. 点击"Compare & pull request"按钮
3. 填写PR信息：
   - **标题**: 简明描述功能
   - **描述**: 详细说明更改内容
   - **审查者**: 选择代码审查人
4. 点击"Create pull request"

### 5. 代码审查
- 审查者检查代码质量
- 提出修改意见
- 通过审查后合并

### 6. 合并PR
- 使用"Merge pull request"按钮
- 选择合并方式（Merge/Squash/Rebase）
- 删除功能分支

### 7. 清理分支
```bash
# 删除本地分支
git branch -d feature/new-feature

# 删除远程分支
git push origin --delete feature/new-feature
```

## ⚙️ GitHub分支保护设置

### 设置步骤
1. 进入GitHub仓库 → Settings → Branches
2. 点击"Add rule"
3. 填写规则：
   - **Branch name pattern**: `main`
   - **Require pull request reviews**: 勾选
   - **Require status checks**: 可选
   - **Include administrators**: 勾选（保护管理员）

### 保护规则
- ✅ **必需PR审查**: 所有更改都需要审查
- ✅ **阻止直接推送**: 禁止直接推送到main
- ✅ **需要CI通过**: 可选，要求测试通过
- ✅ **代码所有者审查**: 可选

## 🛠️ 常用命令

### 分支操作
```bash
# 查看所有分支
git branch -a

# 切换分支
git checkout branch-name

# 创建并切换分支
git checkout -b new-branch

# 删除分支
git branch -d branch-name
git push origin --delete branch-name
```

### 同步操作
```bash
# 拉取最新代码
git pull origin main

# 推送分支
git push -u origin feature/branch

# 变基到最新main
git checkout feature/branch
git rebase main
```

### PR相关
```bash
# 查看分支差异
git diff main..feature/branch

# 压缩提交
git rebase -i HEAD~3

# 强制推送（谨慎使用）
git push --force-with-lease
```

## 📝 提交信息规范

### 格式
```
<type>(<scope>): <subject>

<body>

<footer>
```

### 类型
- `feat`: 新功能
- `fix`: 修复bug
- `docs`: 文档更新
- `style`: 代码格式调整
- `refactor`: 重构
- `test`: 测试相关
- `chore`: 构建过程或工具配置

### 示例
```
feat: 添加用户登录功能

- 实现用户名密码登录
- 添加JWT令牌验证
- 更新API文档

Closes #123
```

## 🔄 冲突解决

### 解决步骤
```bash
# 拉取最新代码
git checkout main
git pull origin main

# 切换到功能分支
git checkout feature/branch

# 变基解决冲突
git rebase main

# 如果有冲突，手动解决后
git add .
git rebase --continue

# 强制推送
git push --force-with-lease
```

## 🎯 最佳实践

### 1. 小步提交
- 每个提交只做一件事
- 提交信息清晰明了
- 定期提交，避免丢失工作

### 2. 及时同步
- 每天开始工作前拉取最新代码
- 定期推送分支到远程
- 避免分支差异过大

### 3. 代码审查
- 认真填写PR描述
- 及时响应审查意见
- 保持积极的沟通态度

### 4. 分支管理
- 功能完成后及时删除分支
- 保持分支列表整洁
- 使用有意义的 branching 命名

## 🚨 注意事项

1. **不要在main分支上直接开发**
2. **定期备份重要分支**
3. **PR前先解决冲突**
4. **审查通过后再合并**
5. **删除不需要的分支**

## 📚 相关资源

- [GitHub Flow](https://guides.github.com/introduction/flow/)
- [Git分支管理策略](https://nvie.com/posts/a-successful-git-branching-model/)
- [Conventional Commits](https://www.conventionalcommits.org/)

---

## 💡 当前状态

你现在位于分支: `feature/linux-commands-update`
这个分支包含了最新的Linux命令学习资料。

**下一步**: 在GitHub上创建Pull Request进行代码审查！
